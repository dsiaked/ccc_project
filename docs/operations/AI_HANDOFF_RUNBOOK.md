# AI 주도 프로젝트 인수인계 실행서

이 문서는 새 담당자가 AI 코딩 에이전트에게 프로젝트 인수인계를 맡길 때 사용한다.
AI는 저장소를 조사하고, 새 담당자의 환경을 준비하고, 테스트 환경에서 전체 흐름을
검증한 뒤 인수인계 완료 여부를 판정한다.

비밀값 자체는 이 문서, 채팅, 로그, Git에 기록하지 않는다.

## 1. 새 담당자가 AI에 전달할 시작 프롬프트

아래 내용을 새 AI 작업 세션의 첫 요청으로 그대로 전달한다.

```text
이 저장소의 AI_HANDOFF_RUNBOOK.md를 읽고 프로젝트 인수인계를 주도해줘.

목표:
- 내가 기존 담당자의 도움 없이 로컬 실행, 테스트 환경 운영, 배포, 장애 확인,
  관리자 계정 관리, 최적화 작업 실행을 수행할 수 있는 상태를 만든다.
- 실제 운영 환경은 내가 명시적으로 승인하기 전까지 변경하지 않는다.
- 비밀값은 출력하거나 Git에 기록하지 않는다.

진행 방식:
1. PROJECT_ACCESS_AND_ACCOUNTS.md와 저장소 설정을 읽고 현재 상태를 조사한다.
2. 인수인계 계획과 필요한 사람 작업을 체크리스트로 제시한다.
3. 자동 수행 가능한 작업은 직접 수행하고 결과를 검증한다.
4. 로그인, MFA, 초대 수락, 비밀값 입력처럼 사람만 가능한 작업은 한 번에 하나씩
   정확한 위치와 필요한 최소 권한을 설명하며 요청한다.
5. 각 단계 완료 증거를 HANDOFF_STATUS.md에 비밀값 없이 기록한다.
6. 테스트 환경에서 전체 검증이 끝나기 전에는 운영 배포를 요청하지 않는다.
7. 완료 기준을 모두 충족하면 남은 위험과 권한 회수 목록을 보고한다.

작업 중 발견한 코드나 설정 결함은 테스트와 함께 수정하되, 운영 데이터 변경,
권한 확대, 키 교체, 외부 서비스 배포는 반드시 내 승인을 받은 후 진행한다.
```

## 2. 인수인계 시작 전 사람이 준비할 것

AI 세션을 시작하기 전에 기존 담당자와 새 담당자가 아래 항목을 준비한다.

- 새 담당자 개인 계정으로 GitHub 저장소 초대 수락
- 새 담당자 개인 계정으로 Supabase 운영 및 테스트 프로젝트 초대 수락
- Firebase/Google Cloud 프로젝트 초대 수락
- Kakao Developers 앱 관리 권한 수락
- 애플리케이션의 새 담당자 계정에 `global_admin` 역할 부여
- 조직 비밀번호 관리자 또는 승인된 Secret 저장소 접근 권한
- 운영 변경 승인권자와 비상 연락 수단 확인
- 운영과 분리된 Supabase 테스트 프로젝트 확보

기존 담당자의 개인 계정 비밀번호를 전달하지 않는다. 초대와 역할 부여 방식으로
소유권을 이전한다.

## 3. AI가 반드시 지켜야 할 안전 규칙

AI는 다음 규칙을 모든 작업보다 우선한다.

1. `.env`, 서비스 계정 JSON, API 키의 실제 값을 채팅이나 문서에 출력하지 않는다.
2. 환경변수를 점검할 때는 변수 이름과 설정 여부만 확인한다.
3. `SUPABASE_SERVICE_ROLE_KEY`에 `VITE_` 접두사를 붙이지 않는다.
4. 운영 DB 초기화, 사용자 삭제, 키 교체, IAM 변경, 운영 배포는 사람의 명시적
   승인 없이 수행하지 않는다.
5. 시뮬레이션과 파괴적 검증은 분리된 테스트 Supabase 프로젝트에서만 수행한다.
6. 기존 담당자의 권한은 새 담당자의 검증 완료 전까지 회수하지 않는다.
7. 외부 서비스에는 가능한 최소 권한만 요청한다.
8. 실패를 숨기지 않고 재현 명령, 원인, 해결 상태를 기록한다.
9. 운영 환경과 테스트 환경의 프로젝트 ID를 작업 전마다 확인한다.
10. Git의 기존 미커밋 변경을 임의로 되돌리지 않는다.

## 4. AI의 작업 단계

AI는 아래 단계를 순서대로 진행한다. 각 단계가 끝날 때
`HANDOFF_STATUS.md`에 결과와 증거를 기록한다.

### 단계 A: 저장소 및 로컬 환경 조사

AI가 직접 수행한다.

- `PROJECT_ACCESS_AND_ACCOUNTS.md`, `README.md`, `package.json` 확인
- Git 브랜치, 원격 저장소, 미커밋 변경 확인
- 필요한 환경변수 이름 수집
- Node.js, npm, Git, Python, Supabase CLI, Firebase CLI, gcloud, Docker 설치 확인
- `.env`와 `.env.local`이 Git에서 추적되지 않는지 확인
- 운영/테스트 Supabase 프로젝트 식별

통과 기준:

- 필요한 도구와 미설치 도구가 명확히 기록됨
- 비밀값 노출 없이 환경변수 설정 여부가 기록됨
- 운영과 테스트 대상이 구분됨

### 단계 B: 권한 및 계정 확인

AI는 필요한 계정 목록을 제시하고, 사람에게 로그인이나 초대 수락을 요청한다.
로그인 완료 후 AI는 가능한 범위에서 접근 여부를 검증한다.

확인 대상:

- GitHub: 저장소 읽기/쓰기와 Actions 확인 권한
- Supabase: 운영/테스트 프로젝트 조회, SQL, Functions, Secrets 관리 권한
- Firebase/GCP: Hosting 및 필요한 Google Cloud 리소스 접근 권한
- Kakao Developers: 앱 설정 조회/변경 권한
- 애플리케이션: 새 담당자의 `global_admin` 로그인

통과 기준:

- 서비스별 새 담당자 개인 계정과 역할이 기록됨
- 기존 담당자 개인 비밀번호 공유 없이 접근 가능
- 누락 권한과 승인 담당자가 기록됨

### 단계 C: 로컬 실행 및 기본 품질 검증

AI가 직접 수행하고 오류가 있으면 수정·검증한다.

```powershell
npm install
npm run lint
npm run test:unit
npm run build
npm run dev
```

브라우저에서 확인할 항목:

- 일반 로그인 및 회원가입 화면
- Kakao 로그인 진입
- 신청 화면과 Kakao 지도 검색
- 관리자 로그인
- 권한별 관리자 경로 보호

통과 기준:

- lint, unit test, build 성공
- 로컬 앱 접근 성공
- 콘솔의 치명적 오류 없음

### 단계 D: 테스트 Supabase 환경 검증

반드시 운영과 분리된 테스트 프로젝트에서 진행한다.

- DB 스키마와 최신 마이그레이션 상태 확인
- 필요한 Edge Functions와 Secrets 설정 여부 확인
- 새 담당자 테스트 `global_admin` 계정 확인
- 시뮬레이션 또는 소규모 테스트 데이터 생성
- 일반 사용자, 캠퍼스 회계 순장님, 탑승 관리 간사님, 전체 관리자 권한 흐름 확인

주의:

- 새 DB 검증 전 `node scripts/sync-combined-supabase-setup.mjs`와 `npm test`를
  실행해 통합 설치 SQL이 최신 후속 패치와 일치하는지 확인한다.

통과 기준:

- 새 테스트 프로젝트에서 초기 구축 또는 마이그레이션 성공
- 역할별 허용 및 거부 동작 확인
- Edge Functions 호출 권한 확인

### 단계 E: 핵심 운영 흐름 리허설

테스트 환경에서 새 담당자가 직접 UI를 조작하고, AI가 옆에서 검증한다.

1. 일반 사용자 계정 생성 및 로그인
2. 신청 생성과 수정
3. 캠퍼스 회계 순장님 입금 확인 및 본부 송금 보고
4. 전체 관리자 송금 확인
5. 배차 최적화 작업 생성 및 워커 실행
6. 배차 확정과 개인 버스표 확인
7. 탑승 관리 간사님 지정, 탑승 처리, 버스 출발 처리
8. Google Sheet 명단 동기화
9. 감사 로그 확인

통과 기준:

- 흐름별 성공 결과와 실패 시 권한 차단 결과가 기록됨
- 새 담당자가 각 작업의 로그 위치와 복구 방법을 설명할 수 있음

### 단계 F: 배포 및 복구 리허설

먼저 테스트 또는 미리보기 채널에서 수행한다.

- GitHub PR 생성 및 Actions 검증
- Firebase Hosting 미리보기 배포
- Supabase Edge Function 한 개 재배포 및 호출 확인
- 배포 실패 로그 확인
- 이전 정상 버전으로 복구하는 절차 확인
- 선택적으로 Cloud Run 최적화 Job 실행 확인

운영 배포는 새 담당자가 결과를 확인하고 명시적으로 승인한 경우에만 진행한다.

통과 기준:

- 새 담당자 계정으로 배포 가능
- 실패 원인 확인과 복구 가능
- 배포에 사용된 서비스 계정과 Secret 위치가 기록됨

### 단계 G: 소유권 이전 마무리

모든 테스트를 통과한 뒤에만 수행한다.

- 새 담당자를 포함한 최소 2명의 조직 관리자 유지
- 기존 담당자의 불필요한 개인 권한 회수
- 공유되거나 노출 가능성이 있는 키 교체
- GitHub Actions, Supabase Secrets, Google Secret Manager 값 갱신
- 서비스별 접근자 목록과 MFA 상태 최종 확인
- 남은 기술 부채와 다음 점검일 기록

통과 기준:

- 새 담당자가 기존 담당자 도움 없이 테스트 환경 구축, 배포, 복구를 완료함
- 기존 담당자 권한 회수 후에도 서비스 운영 가능
- 남은 위험과 책임자가 명확함

## 5. 사람 승인이 반드시 필요한 작업

AI는 아래 작업 직전에 정확한 변경 내용과 영향 범위를 설명하고 승인을 기다린다.

- 운영 Supabase 프로젝트의 SQL 실행 또는 마이그레이션 적용
- 운영 사용자 생성, 삭제, 역할 변경
- 운영 Edge Function 배포 및 Secret 변경
- Firebase Hosting 운영 채널 배포
- Google Cloud IAM 변경, 서비스 계정 키 발급 또는 폐기
- `SUPABASE_SERVICE_ROLE_KEY` 등 관리자급 키 교체
- 기존 담당자 권한 회수
- 비용이 발생하거나 데이터 삭제 가능성이 있는 작업

## 6. HANDOFF_STATUS.md 기록 형식

AI는 인수인계 시작 시 저장소 루트에 `HANDOFF_STATUS.md`를 만들고 아래 형식으로
진행 상황을 갱신한다. 실제 이메일, 키, 비밀번호는 기록하지 않는다.

```markdown
# 인수인계 진행 상태

## 환경
- 시작일:
- 새 담당자:
- 운영 Supabase 프로젝트: 확인됨/미확인
- 테스트 Supabase 프로젝트: 확인됨/미확인

## 체크포인트
| 단계 | 상태 | 증거 | 남은 작업 |
| --- | --- | --- | --- |
| A. 로컬 조사 | 대기 |  |  |
| B. 계정 및 권한 | 대기 |  |  |
| C. 로컬 실행 | 대기 |  |  |
| D. 테스트 Supabase | 대기 |  |  |
| E. 운영 흐름 리허설 | 대기 |  |  |
| F. 배포 및 복구 | 대기 |  |  |
| G. 소유권 이전 | 대기 |  |  |

## 승인 기록
| 시각 | 승인 작업 | 승인자 | 결과 |
| --- | --- | --- | --- |

## 남은 위험
- 없음

## 최종 판정
- 인수인계 완료/미완료:
- 미완료 사유:
```

## 7. AI의 최종 완료 보고 형식

AI는 모든 작업이 끝나면 다음 형식으로 보고한다.

```text
인수인계 판정: 완료 / 조건부 완료 / 미완료

검증 완료:
- 로컬 실행:
- 테스트 환경 구축:
- 역할별 권한:
- 핵심 운영 흐름:
- 배포:
- 복구:

남은 작업:
- 작업 / 책임자 / 기한

회수 또는 교체할 접근 권한:
- 대상 / 조치 / 승인자

확인된 위험:
- 위험 / 영향 / 대응
```

## 8. 관련 문서

- `PROJECT_ACCESS_AND_ACCOUNTS.md`: 서비스별 계정, 역할, Secret, 최소 권한
- `README.md`: 기본 프로젝트 실행 및 Supabase 초기 설정
- `optimizer/README.md`: 로컬 워커와 Cloud Run 최적화
- `sql/README.md`: 데이터베이스 설치 파일과 적용 순서
- `sql/simulation/NEW_SEED_README.md`: 테스트 시뮬레이션 실행
- `supabase/functions/*/README.md`: Edge Function별 설정과 배포
