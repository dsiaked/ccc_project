# Version-Controlled Data

This directory contains small, reviewable data files that belong in source
control.

- `templates/`: blank or reusable import templates
- `samples/`: example or step-specific input data

Generated spreadsheets, previews, reports, and installers belong in
`outputs/`, which is intentionally ignored by Git.
