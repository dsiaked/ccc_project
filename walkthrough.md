# Walkthrough - Progress Timeline, UI Layout & Notice Card Grouping

홈 화면의 전체적인 UI 완성도 향상에 이어, 공지 영역에 중복 노출되는 개인 알림을 하나로 그룹화하여 가독성을 높이는 개선 작업을 마쳤습니다.

## 주요 변경 사항

### 1. 배차 확정 시 타임라인 카드 숨김 처리
- **`HeroSection.tsx`**: 배차가 확정(`isConfirmed`가 `true`)된 상태에서는 홈 화면에서 타임라인 카드를 숨겨 중복 정보를 제거했습니다.

### 2. "신청 마감" 배너 컴팩트화
- **`HomeDeadlineBanner.module.css`**: 배너의 `padding`을 줄이고 요소를 한 줄 공지 바 형태로 배치(`flex-wrap: wrap`)하여 불필요한 스크롤 영역을 해소했습니다.

### 3. 로그인 여부에 따른 아코디언 및 공지사항 순서 동적 변경
- **`HomePage.tsx`**: 로그인 유저에게는 **[공지] 영역이 최상단**, 로그아웃 유저에게는 **[이용 순서] 아코디언이 최상단**에 위치하도록 순서를 동적으로 변환했습니다.

### 4. 문의 답변 알림 그룹화(Grouping) 및 뱃지 카운트 표시
- **`HomeNoticeSection.tsx`**:
  - **카테고리별 그룹화**: 동일한 문의 내에서 답변이 오갈 때 발생하던 개별 알림들을 **문의 분야(예: "기타 문의") 기준으로 하나로 자동 병합**하였습니다.
  - **뱃지 카운트**: 읽지 않은 알림이 2개 이상일 경우, 공지 제목 우측에 `(2)`와 같이 **읽지 않은 알림 개수를 뱃지 형태로 표시**해 줍니다.
  - **일괄 읽음 처리**: 사용자가 병합된 알림 카드를 클릭하여 1:1 문의 내역으로 이동할 때, **해당 문의와 연결된 모든 알림을 한 번에 병합하여 API를 통해 일괄 읽음 처리**합니다.
- **`HomeNoticeSection.module.css`**:
  - 알림 왼쪽 세로선이 간격선 때문에 끊겨 보였던 버그를 가상 요소 `::before`와 `bottom: -1px`을 조합해 **매끄러운 하나의 연속 실선**이 되도록 고쳤습니다.

---

## 검증 및 테스트 결과

### 자동화 테스트
- **TypeScript 타입 체크**: `npx tsc --noEmit` 검사를 성공적으로 완료했습니다.
- **유닛 테스트**: `npm run test:unit` 실행 결과 **694개의 테스트 케이스가 전원 성공(Pass)** 하였습니다.
