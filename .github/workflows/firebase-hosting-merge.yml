# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - dev
concurrency:
  group: firebase-hosting-live-dev
  cancel-in-progress: true
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_KAKAO_JAVASCRIPT_KEY: 293061342121c112289ab4320f6d2245
          VITE_SIMULATION_PROJECT_ID: pjbvxoesgwhbxfsfjliw
          VITE_CCC_SUMMER_BASE_URL: https://ccc-summer.vercel.app
          VITE_CCC_SUMMER_CLIENT_ID: bus-seoul-return
        run: |
          test -n "$VITE_SUPABASE_URL"
          test -n "$VITE_SUPABASE_ANON_KEY"
          test -n "$VITE_KAKAO_JAVASCRIPT_KEY"
          test -n "$VITE_SIMULATION_PROJECT_ID"
          test -n "$VITE_CCC_SUMMER_BASE_URL"
          test -n "$VITE_CCC_SUMMER_CLIENT_ID"
          npm ci
          npm run lint
          npm run db:migrations:verify
          npm run test:unit
          python -m pip install -r optimizer/requirements-dev.txt
          cd optimizer
          python -m pytest tests -q
          cd ..
          npm run build
      - name: Verify Supabase deployment compatibility
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run deploy:check:supabase
      - name: Deploy changed Edge Functions
        if: ${{ env.SUPABASE_ACCESS_TOKEN != '' }}
        run: |
          npx supabase functions deploy ai-operations-report --project-ref qdpfccuqeumguhishifu
          npx supabase functions deploy allocation-optimizer-launcher --project-ref qdpfccuqeumguhishifu
          npx supabase functions deploy ccc-summer-handoff --project-ref qdpfccuqeumguhishifu
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CCCBUS_99680 }}
          channelId: live
          projectId: cccbus-99680
